import { ExtractGoogleDriveLinkPipe } from './extract-google-drive-link.pipe';

describe('ExtractGoogleDriveLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractGoogleDriveLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
